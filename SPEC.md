# netryx_demo — 局域网设备与连接管理 (需求草案 v0.1)

> 状态:需求对齐中(待确认项见文末"待对齐问题")
> 更新:2026-09-07

## 1. 背景

家庭同一 WiFi(实测网段 `192.168.3.0/24`,网关/路由为华为 **WS5800-10 "华为路由A2"**,
本机 `192.168.3.235` Windows 11)下,需要:

- 识别**路由器**及**接入路由器的各设备**(设备A、设备B …)
- 识别设备**开放端口与服务**
- 梳理**连接关系**(谁接入路由、谁在线、设备间关系拓扑)
- **手机端**查看与管理(网页或简易 App)
- 约束:**少用 Docker**;优先借用成熟库/方案(调研已完成,见下)

## 2. 调研结论(已完成,2026-09-07 实测验证)

候选评估结论:**直接复用 [netryx](https://github.com/UbhiTS/netryx)(MIT,纯 Python 标准库,零依赖)**,
本机 Python 直接运行,无需 Docker。实测证据(在本机真实网络 `192.168.3.0/24` 上):

| 能力 | 实测结果 |
|---|---|
| 设备发现 | 扫描出 3 台:华为路由(网关+DNS)、随机 MAC 设备(手机隐私地址)、本机 |
| 设备识别 | 华为路由识别出 UPnP 型号 **WS5800-10 "华为路由A2"**;mDNS/SNMP/NetBIOS/SSDP、TTL 系统猜测、OUI 厂商库(内置 107 条,可下载全量 IEEE OUI) |
| 端口扫描 | 路由 Quick 扫描:25/53/80/110/143/443;80→可点击 URL,443→抓到 TLS 证书(`mediarouter.home`,TLSv1.3) |
| 连接梳理 | 表格/卡片/交互拓扑图视图(多布局);网关、在线状态、跳数 |
| 手机显示 | 390px 窄屏实测可用;已做移动端适配(修复 Scan 按钮溢出) |
| 运行方式 | `python netryx.py` 零依赖;`--host 0.0.0.0` 手机可访问;自带登录(默认 admin/admin) |
| 附加能力 | Live monitor 定时重扫+新设备通知、基线+rogue 告警(webhook/MQTT)、WoL 唤醒、自定义改名备注、扫描历史、CSV/JSON 导出、OpenAPI、CLI 一次性扫描、MCP |

> 调研产物详见 `docs/research-netryx.md`;实测截图 `shots/`(手机端登录页/仪表盘/扫描中)。

## 3. 需求草案

### 功能需求

- **F1 设备发现与识别**:扫描网段;输出设备名/厂商/型号/系统类型(路由/手机/PC/IoT...)与在线状态
- **F2 端口与服务**:开放端口、服务名、banner、Web 管理地址可点击直达
- **F3 连接梳理**:设备列表 + 拓扑图(接入关系、网关、在线/离线)
- **F4 手机端查看**:同一 WiFi 下手机浏览器访问;符合"手机能显示情况"
- **F5 设备管理动作**:自定义设备名/备注(标记)即可,或延伸告警(新设备/新端口/暴露风险)、WoL 唤醒
- **F6 数据记录**:扫描历史、事件日志、known-good 基线对比

### 非功能需求

- 数据只在本机,不出网
- 登录鉴权(默认 admin/admin,部署时改密)
- 低资源占用;Windows 本机常驻或按需启动
- 免 Docker 运行

## 4. 待对齐问题

1. **常驻方式**:电脑开机自启常驻(手机随时看)还是按需手动启动?
2. **管理深度**:"管理"= 只读查看+改名备注即可,还是需要主动动作(告警推送、WoL、踢设备?)——踢设备需路由配合,华为消费级路由无开放管理 API,预计不可行
3. **手机端形态**:直接网页(已适配,零额外工作)够,还是需要 PWA(可安装+通知)/独立简易 App?
4. **设备清单**:路由下实际接入设备有哪些、各自叫什么(方便 Demo 演示时命名标注)?
5. **安全**:保持登录页+改密码,还是信任内网直接免登录(仅限可信网段)?
