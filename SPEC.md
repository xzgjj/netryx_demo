# SPEC — 局域网设备与连接管理系统(netryx_demo)

> 版本:v2 重设计(2026-09-07)
> 性质:实验性项目;实验场=家庭 WiFi(192.168.3.0/24,华为 WS5800-10);目标场=公司/客户现场(Windows + Python 3.8+)
> 基底决策(拷问已确认):复用 netryx 引擎(零改动,零第三方依赖,MIT),UI 层做裁剪瘦身 + 表层中文;启动器按需手动;功能范围=查看 + 设备改名备注;手机端=浏览器直接访问。
> 贯穿铁律:①数值参数无法推导者标注 `[需实测]`,严禁臆造 ②每功能规格章节 ≥3 示例(正常/边界/错误) ③架构说清"谁生产数据、谁消费数据、谁持有状态"。

---

## 1. 项目目标概述

### 1.1 一句话定义

一个运行在局域网内某台 Windows 上的**自托管 Web 应用**:扫描并识别同一 WiFi 下的路由器与全部接入设备、探测开放端口与服务、以表格/拓扑呈现连接关系,手机浏览器即可查看并为设备改名备注;引擎零第三方依赖、免 Docker。

### 1.2 项目目标

| 目标 | 说明 | 衡量 |
|---|---|---|
| G1 设备可见 | 同一 WiFi 下所有接入设备(含网关/路由)被发现、识别(厂商/型号/类型/系统) | 扫出 ≥ 实际在线设备数,网关必被识别为 Router |
| G2 服务可见 | 每台设备开放端口、服务名、Web 管理地址可点击直达 | Quick 扫描后端口列表非空(对存活主机) |
| G3 关系可见 | 谁接入路由、谁在线、谁是新面孔,以拓扑图直观呈现 | 拓扑图节点=设备、中心=网关 |
| G4 手机可看 | 手机浏览器(同一 WiFi)完成全部查看与改名操作 | 真机实测通过(390px 级布局可用) |
| G5 现场可部署 | 公司/客户现场照文档三步跑起来,不绑定任何实验环境信息 | 文档冒烟:新机器装 Python→启动→手机访问 |
| G6 只读+轻管理 | 不做踢设备/告警推送等主动控制;改名备注持久化 | 重启后改名仍在 |

### 1.3 背景与交付物

- 背景:用户需要"同一 WiFi 下设备与网络连接管理、手机能显示"的工具;调研后选定 netryx(实测验证)为基底;先在家实验,最终跑在公司/客户现场。
- 交付物清单:
  1. `engine/` — netryx 上游引擎原样(netryx.py)+ 定制 UI(ui.html,裁剪+表层中文),并附 `UPSTREAM.md`(上游 commit、改动清单)
  2. `start.ps1` / `start.cmd` — 按需启动器(动态检测,不写死任何机器信息,含改密提醒)
  3. `docs/DEPLOY.md` — 现场部署三步文档(装 Python → 启动 → 手机访问+改密)
  4. `README.md` — 简介与快速开始
  5. 交付验收记录(真机手机实测、接口冒烟结果)
- 非交付:引擎源码修改、Docker 方案、移动端 App、告警/踢设备功能、任何真实环境数据入库(`shots/` 仅本机保留,`.gitignore` 排除;仓库为 public)。

### 1.4 项目需求(汇总)

- 功能:①启动器与环境自检 ②登录认证(session 30 天) ③网络上下文展示(本机/网关/网段) ④扫网段(手动触发、进度反馈、可取消) ⑤设备识别(协议链) ⑥端口与 Web URL ⑦列表/卡片/拓扑查看(搜索过滤排序) ⑧改名备注(持久化) ⑨Live monitor(可选勾选)+桌面通知 ⑩日志与诊断
- 隐藏(引擎保留、UI 不露面):Baseline/Events 管理页、API Tokens、OpenAPI/MCP 面板、计划任务(Schedule)、OUI 下载按钮、Workers 设置、WoL 按钮、扫描历史面板、SNMP/mDNS 开关(默认全开)
- 非功能:数据不出网、登录鉴权、单作业互斥、中文表层界面、手机适配、免 Docker、零第三方依赖、可迁移(换机器即用)

---

## 2. 架构与核心约束

### 2.1 设计抽象(三层)

```
┌─────────────────────────────────────────────────────────┐
│ 表现层  ui.html(定制:裁剪+中文)  ← 手机/桌面浏览器        │
│         视图:登录页/仪表盘/列表/卡片/拓扑/详情/设置(精简)   │
├─────────────────────────────────────────────────────────┤
│ 服务层  netryx.py 内置 HTTP 服务(标准库 ThreadingHTTPS)  │
│         /login /api/*(认证、扫描作业、设备、历史、导出)    │
│         SSE /api/events/stream;HTML 由 ui.html 提供      │
├─────────────────────────────────────────────────────────┤
│ 引擎层  netryx.py 扫描器(线程池):ICMP→TCP→ARP→mDNS→SNMP→ │
│         UPnP→TLS→OUI→OS猜测;端口扫描;风险评分             │
│         单进程内存 JOBS / LAST_RESULTS + JSON 持久化      │
└─────────────────────────────────────────────────────────┘
```

核心抽象:

- **作业(Job)**:一切编排的基本单位。discovery / portscan / oui 三种;`JOBS[id]` 持有运行时状态(进度、部分结果、取消标志)。
- **设备(Device)**:不可变扫描结果对象(见 2.4),身份键 `key = mac(小写) or ip`(无 MAC 时回退 IP)。
- **元数据(Meta)**:人给的设备身份扩展(友好名 name、备注 notes),键同 Device.key,持久化于 `devices.json`,扫描结果展示前经 `apply_meta` 合并。
- **历史快照(History)**:每次扫描全量快照文件 `history/scan_<ts>.json`,滚动保留最近 50 份。
- **事件(Event)**:`{id,time,kind,severity,data}` 环形日志 + hub,消费方式:SSE/长轮询/查询。
- **认证态(Auth)**:单管理员(PBKDF2)+ 会话 cookie(`ns_session`,30 天),`/api/*` 除 login/info/oui/totp-status 外均需认证。

### 2.2 谁生产、谁消费、谁持有状态(铁律 ③)

| 数据 | 生产者 | 消费者 | 状态持有者 | 生命周期 |
|---|---|---|---|---|
| 设备结果 | 扫描作业(线程池写 `job["devices"]`,完成后置 `LAST_RESULTS`) | 前端(轮询 `/api/job`)、导出、历史快照 | 内存 `JOBS`/`LAST_RESULTS`;快照落盘 history | 作业期间→完成;内存态重启即失,快照持久 |
| 设备 meta(名/备注) | 用户(改名表单 → `POST /api/device`) | 展示层(apply_meta)、导出 | `netryx-data/devices.json`(键=MAC/IP) | 永久,直到用户改/删 |
| 已见设备集合 | 扫描完成时自动维护 | 新设备判定(与 LAST_RESULTS 对比) | `netryx-data/seen.json` | 永久 |
| 历史快照 | 每次扫描完成(含中断 status=stopped) | 历史面板(隐藏,API 保留)、`apply_meta` 演示 | `netryx-data/history/scan_<ts>.json` | 滚动 50 份 |
| 认证材料 | 首次启动种子 / Settings 改密 / env | `verify_admin`、登录限速 | `netryx-data/auth.json`(hash+salt) | 永久 |
| 会话 | `POST /api/login` | 每个 `/api/*` 请求 | 内存 sessions + 浏览器 cookie | SESSION_TTL 30 天 / 登出 |
| 事件 | 检测器(新设备/新端口/暴露/扫描完成) | UI SSE、事件面板(隐藏)、webhook(未启用) | 内存环形 + `events.json` | 内存上限 `[需实测]`,持久追加 |
| 扫描配置(勾选项/网段输入) | 用户操作 | 下次扫描 | `localStorage`(前端)+ `settings.json`(计划任务,隐藏) | 浏览器持久 |

### 2.3 核心约束(不可违反)

1. **引擎零改动**:netryx.py 全量原样(上游升级只换文件)。
2. **零第三方依赖**:运行仅 Python 标准库;Python ≥ 3.8(上游声明)。
3. **免 Docker**:运行 = `python netryx.py`。
4. **不写死环境**:启动器/文档不得含 192.168.3.x、华为路由等实验环境信息(统一动态检测 + 占位)。
5. **真实数据不进公开仓库**:shots、扫描 JSON、日志、auth 均 .gitignore。
6. **单一扫描**:discovery 单实例互斥(busy 时新请求挂同作业)。
7. **认证必在**:默认登录;默认密码 admin/admin 必须被启动器/UI 双重提醒修改。
8. **手机优先**:任何交互在 390px 宽下可用(验收项)。

### 2.4 数据结构(接口契约,来自引擎源码核验)

**设备对象**(扫描结果的设备条目,字段以实测 CLI 输出为准):

```json
{"ip":"192.168.3.1","ttl":64,"latency":28.0,"via":"icmp","mac":"30:a2:c2:77:9c:a6",
 "vendor":null,"random_mac":false,"hostname":null,"os":"Linux / Unix / macOS / mobile",
 "hops":0,"detect":"icmp","is_gateway":true,"is_self":false,"is_dns":true,
 "name":null,"notes":null,"mdns_services":[],"mdns_name":"华为路由A2",
 "model":"WS5800-10 001",
 "upnp":{"friendlyName":"华为路由A2","manufacturer":"Huawei Technologies Co., Ltd.",
         "modelName":"WS5800-10","modelNumber":"001",
         "modelDescription":"Huawei Home Gateway",
         "deviceType":"urn:schemas-upnp-org:device:InternetGatewayDevice:1"},
 "ports":[{"port":80,"service":"HTTP","banner":null,"url":"http://192.168.3.1","new":false,
           "title":"Success"},
          {"port":443,"service":"HTTPS","url":"https://192.168.3.1",
           "tls":{"proto":"TLSv1.3","cipher":"TLS_AES_256_GCM_SHA384",
                  "cn":"mediarouter.home","expires":"2044-04-12","self_signed":false}}],
 "device_type":"Router / Gateway","new":false,"changed":false,
 "risk":{"tier":"none","score":0,"reasons":[]},
 "first_seen":1788743744,"last_seen":1788743744,"seen_count":1}
```

**作业对象**(`GET /api/job?id=N` 返回,源码 `new_job`):

```json
{"id":"3","type":"discovery","status":"running","phase":"Discovering live hosts",
 "total":254,"done":87,"devices":[...部分结果...],"result":null,"error":null,
 "new_devices":[],"new_ports":0,"cancel":false,"stopped":false,"started":1788743744.0}
```

- `status`:running / done / error / canceled(停止置 `stopped=true`)
- `phase`:`Discovering live hosts` → `Identifying devices` → `Scanning ports` → `Complete`

**其余文件/接口契约**:

| 契约 | 形状 |
|---|---|
| `devices.json` | `{"30:a2:c2:77:9c:a6":{"name":"主路由","notes":"客厅"}, ...}` |
| `seen.json` | `["30:a2:c2:77:9c:a6","192.168.3.209", ...]` |
| `history/scan_<ts>.json` | `{"time":ts,"subnet":"192.168.3.0/24","count":3,"source":"manual","status":"complete","devices":[...]}`(保留最近 50) |
| `auth.json` | `{"username":"admin","hash":"<pbkdf2>","salt":"<hex>",...}` |
| `POST /api/login` | in `{username,password,remember?,code?}` → out `{ok:true}` + Set-Cookie `ns_session`;错误 401 `{error}`;限速 429 `{error}` |
| `POST /api/scan` | in `{subnet,scan_ports?,port_profile?("quick"|"extended"|"full"),use_mdns?,use_snmp?,workers?,source?}` → out `{job_id}`;空 subnet→400;busy → `{job_id,busy:true,note}` |
| `POST /api/device` | in `{mac|ip,name?,notes?}` → out `{ok}`;两者皆空→400 `{error:"mac or ip required"}` |
| `GET /api/info` | `{subnet,local_ip,gateway,cpu,platform,oui:{...},auth:{enabled,method,username,default_creds,trust_localhost}}` |
| `GET /api/events/stream` | SSE `text/event-stream`;`?since=<id>` 回放 |
| `GET /api/export?format=csv|json` | 下载当前结果 |
| `POST /api/job/stop` | `{id}` → `{ok:true}` / 404 |

### 2.5 数据流转(端到端)

```
用户点 Scan ──POST /api/scan──▶ start_job("discovery", run_discovery, subnet, ...)
    │                              │JOBS[id] 登记,running
    │◀──{job_id}──────────────────┤线程池:ping sweep→ARP→识别→(端口)→写 job.devices
    │                              │每夜写 LAST_RESULTS;完成:save_history();事件 scan_complete
前端轮询 GET /api/job?id ────────▶│(0.8s 间隔 [需实测调优])──▶ phase/total/done/devices 增量渲染
    ▼
进度条/统计卡/设备表实时刷新 → 点击设备 ──POST /api/device──▶ save_device_meta → devices.json
    │                              │LAST_RESULTS 同步更新 name/notes;次日扫描 apply_meta 合并
    ▼
手机查看/拓扑图(前端由 devices 生成节点边 → d3 风格物理布局[引擎内置])
```

---

## 3. 业务详细规格(核心模块)

> 每个模块含:功能/触发、流程与规则、边界与转换、**≥3 示例(正常/边界/错误)**、错误处理。
> 用户交互统一反馈语言:三态(进行中=蓝/完成=绿/失败=红)+ 稳定文案(登录失败/扫描中/保存成功/保存失败)。

### 3.0 核心用户场景与行为路径(设计总览)

**场景 A · 家里实验(首次跑通)**
1. 双击 `start.cmd` → 控制台显示"检测到的 IPv4 地址列表 + 手机访问地址 + 改密提醒"(状态:服务启动中)
2. 电脑浏览器打开 `http://127.0.0.1:8765` → 登录页(中文)→ 输 admin/admin → 仪表盘(顶部状态卡:本机 IP/网关/网段/平台)
3. 点 `扫描网络` → 按钮变红色 `停止扫描`,进度行"发现存活主机 12/254 · 约 1 分钟" → 完成:统计卡(设备数/带Web数/开放端口数)+ 表格列出 3 台(网关/手机/本机)
4. 点网关行 → 详情 → 端口 80/443(蓝链)→ 点开路由 Web;点"改名"输入"客厅主路由" → 保存 → toast"已保存",行内名称更新
5. 刷新浏览器/重启服务再扫 → 名称仍在(devices.json 持久化)

**场景 B · 日常查看(手机)**
1. 手机连同一 WiFi → 浏览器打开 `http://<电脑IP>:8765`(之前登录过,session 30 天,直达仪表盘)
2. 顶部状态卡显示"检测网段 192.168.3.0/24 · 上次扫描 5 分钟前" → 点 `重新扫描`
3. 列表出现新手机(名称=苹果手机)→ 点开 → 端口空(手机防护)→ 标红风险提示"无";返回点赞即可
4. 想持续盯:勾选 `实时监控 5 分钟` → 后台重扫,新设备到达弹桌面通知

**场景 C · 现场部署(交付)**
1. 客户机器装 Python(勾选 Add to PATH)→ 拷项目文件夹 → 双击 start.cmd
2. 首次启动控制台醒目提示:默认账号 admin/admin,请登录后到设置改密码
3. 手机同 WiFi 打开提示地址 → 登录 → 看到客户网络真实设备 → 完成交付冒烟

### 3.1 模块 M1 启动器与环境自检

- **功能**:一键启动服务;运行前自检 Python/端口/网络环境;输出手机访问地址与安全提醒。
- **触发**:双击 `start.cmd`(包装 `start.ps1`)。
- **流程**:①解析脚本目录,定位 `engine/netryx.py`+`ui.html` ②`python --version` 检查 ≥3.8(`[需实测]`确认现场机型)③检测 8765 端口占用,占用则提示并退出(不自动换端口,避免"服务在哪"困惑)④列举 IPv4(排除 127./169.254./虚拟网卡可加"请选 192.168.x.x 那条"提示)⑤`python netryx.py --host 0.0.0.0 --port 8765` 前台运行(窗口即控制台)。
- **处理规则**:不写死 IP(动态);不自动开浏览器(手机为先,文档说明本条);退出口径统一。
- **边界**:无 Python → 提示安装地址与"勾选 Add to PATH";有多个 Python(3.13 vs 2.x)→ 用 `py -3` 探测链路([需实测]现场);端口被占 → 提示换 `--port`;
- **示例**(3 条):
  - 正常:双击 → 控制台:地址列表 `192.168.3.235`,手机访问 `http://192.168.3.235:8765`,"请先在 Settings 修改默认密码" → 服务前台开始输出日志。
  - 边界:无 Python → `[错误] 未找到 Python 3.8+:请安装 https://www.python.org/downloads/ 并勾选 Add Python to PATH` 后重试。
  - 错误:端口被占 → `[错误] 8765 已被占用(可能已在运行),关闭旧窗口或用 --port 换端口`;exit 1。
- **错误处理**:环境问题全部一次性给出可行动修复指引,不静默。

### 3.2 模块 M2 用户认证与安全

- **功能**:登录/登出/会话保持;阻止默认口令被长期使用;登录限速。
- **触发**:访问任意受保护页面未带有效 `ns_session`;登出;登录(含 2FA 分支,保留但未启用 UI 引导)。
- **流程**:前端 login 页 → `POST /api/login {username,password,remember:true}` → 成功 Set-Cookie(30 天)→ 仪表盘;失败 401 显示"用户名或密码错误";连续失败触发 429(等待若干分钟,数值 `[需实测]`,源码 login_blocked 已实现)。
- **处理规则**:`default_creds` 检测(源码 `is_default_admin()`);UI 顶部常驻黄色警告条"正在使用默认密码,请前往设置修改"直至改密。
- **边界**:session 过期(30 天/登出)再访问 → 无感跳登录页并回跳原视图;2FA 未启用不出现 code 框;admin 改密后旧 session 是否吊销(`[需实测]`,源码 drop_session 语义)。
- **示例**:
  - 正常:POST /api/login {username:admin,password:admin,remember:true} → 200 {ok:true} + Set-Cookie ns_session(30d)→ 仪表盘加载。
  - 边界:密码正确但用户名大小写错误 → 401 {error:"invalid username or password"}(不区分提示类型,防枚举)。
  - 错误:连续 5 次失败 → 429 {error:"too many attempts — wait a few minutes"}(文案前端本地化为中文;等待时长 `[需实测]`)。
- **错误处理**:401/429 均有中文界面反馈;登录页保留"重试"而不使页面卡死。

### 3.3 模块 M3 网络上下文感知(状态卡)

- **功能**:展示"当前在哪"——本机 IP、网关、检测网段、平台、引擎身份;供用户自检扫错网段。
- **触发**:仪表盘加载(`/api/info`),及每次扫描完成刷新。
- **流程**:GET /api/info → 渲染顶部状态卡;刷新即重取。
- **处理规则**:多网卡时提示"以 192.168.x.x 为准"(启发式,不隐藏其他地址);网关识别失败(中文系统 `ipconfig` 输出"默认网关"与源码正则不匹配 → `default_gateway()` 可能返回 None)时,显示"未识别网关"以 `-` 呈现,并靠扫描结果里的 `is_gateway` 设备兜底(实测 CLI 中网关识别成功,依赖 ARP/表路由,`[需实测]` 全场景确认)。
- **示例**:
  - 正常:`{"subnet":"192.168.3.0/24","local_ip":"192.168.3.235","gateway":"192.168.3.1","platform":"Windows 11","auth":{"default_creds":true}}` → 卡片"网段 192.168.3.0/24 · 网关 192.168.3.1 · Windows 11" + 黄色警告(默认密码)。
  - 边界:虚拟网卡优先(如 192.168.56.1)→ 状态卡列全部地址并醒目标注"主网卡建议",扫描框预填 default_subnet(实测为 192.168.3.0/24,正确)。
  - 错误:断网(无默认路由)→ local_ip 回退 127.0.0.1,subnet=192.168.1.0/24 → 状态卡提示"网络异常:请确认已连接路由器",扫描预填值可手改。
- **错误处理**:网络异常不阻塞界面,仍显示"上次结果"。

### 3.4 模块 M4 扫描发起与作业调度

- **功能**:触发整网扫描;进度可见;可取消;防并发重复。
- **触发**:手动"扫描网络"按钮;Live monitor 定时(可选)。
- **流程**:校验并归一化输入 → `POST /api/scan` → 若 busy 返回既有 job_id(挂接)→ 前端进入扫描态;轮询 `/api/job?id=` 渲染进度。
- **处理规则**:输入归一化(去空格;cidr/单 IP/范围混合支持;非法 → 400 中文提示);`port_profile` 白名单校验,非法回退 quick;`workers` auto|128..1000;端口勾选联动 profile;完成时统计卡与"新设备"徽标(与 seen 对比)。
- **边界**:同网段重复提交(挂接不重复扫);扫描中页面刷新(重新 attach 同 job,job 在 JOBS 内存存活);扫描中关闭浏览器(作业继续,下次打开可见最后结果)。
- **示例**:
  - 正常:POST /api/scan {subnet:"192.168.3.0/24",scan_ports:true,port_profile:"quick",source:"manual"} → 200 {job_id:"3"};轮询 job → {"phase":"Discovering live hosts","total":254,"done":120}渲染"120/254"。
  - 边界:重复提交 → 200 {job_id:"3",busy:true,note:"a scan is already running"} → 前端进入同一进度视图并弹出"扫描已在运行"。
  - 错误:subnet 为空 → 400 {error:"subnet required"} → 前端聚焦输入框并提示"请输入网段";点停止 → POST /api/job/stop → 完成时 status=done 且 stopped=true,统计卡标注"已停止"。
- **错误处理**:网络请求失败(引擎未启动)→ 前端重试 2 次后提示"服务无响应,请检查启动器"。

### 3.5 模块 M5 设备发现与识别(协议链)

- **功能**:找到所有存活主机并识别"它是谁"。
- **触发**:作业调度签发 discovery job。
- **流程/规则**(引擎事实):①并发 ping(700ms 超时 [实测源码])+ TCP fallback(挡 ping 设备)②ARP 表解析 MAC ③协议栈识别:mDNS/Bonjour(设备名/服务类型)→ SNMP v2c(交换机/打印机/AP)→ NetBIOS(Windows 名)→ SSDP/UPnP(型号)→ TLS 证书(HTTPS 服务身份)→ OUI 厂商(内置 107 条,可下载全量)→ TTL OS 猜测 → 设备类型猜(组合投票)→ 延迟。
- **边界**:无管理员权限时 ARP 不完整(UI 第一屏"实验提示"说明);随机 MAC(手机隐私地址)标记 `random_mac:true` 并提示"可改名标注";识别字段为 null 时显示"—"。
- **示例**:
  - 正常:路由器 → {"mdns_name":"华为路由A2","model":"WS5800-10 001","upnp":{...Huawei...},"device_type":"Router / Gateway","is_gateway":true} → 列表行显示"华为路由A2 · 路由/网关"徽标。
  - 边界:手机(隐私 MAC)→ {"mac":"66:9d:17:b1:54:2b","random_mac":true,"vendor":null,"os":"Linux / Unix / macOS / mobile"} → 行显示"未知设备(隐私地址)"+ 提示改名。
  - 错误:全网无响应(网关在线但客户端全离线)→ devices 仅含网关+本机;统计卡"3 台设备",不报错(静默降级)。
- **错误处理**:主链路失败(ICMP 全被禁)→ TCP fallback 自动接管;两者皆失败 → 空结果 + 日志记录原因,UI 显示"未发现设备,请检查网段或防火墙"。

### 3.6 模块 M6 端口扫描与服务识别

- **功能**:对存活设备探测端口、识别服务、生成可点击 URL、暴露分级。
- **触发**:网段扫描勾选"扫描端口"(profile=quick 默认);或设备详情"扫描端口"按钮(单机,extended 默认)。
- **流程/规则**:并发 TCP connect(响应超时/并发数 `[需实测]`);Quick≈90 / Extended 1-1024 / Full 1-65535(慢,单机用);服务名映射;banner 抓取;HTTP(S) 端口 → 点击 URL(80/443/8080/8443/8123…);TLS 抓证书(CN/有效期/自签);风险评分(telnet/RDP/SMB/VNC/裸库 → 保险色带 none→critical)。
- **边界**:端口全关(手机)→ 空列表,显示"无开放端口";Full 长耗时(65k 端口/台,提示"可能需要数分钟 `[需实测]`");半开状态扫描中页面刷新。
- **示例**:
  - 正常:路由器 Quick → [25 SMTP,53 DNS,80 HTTP(→链接),110 POP3,143 IMAP,443 HTTPS(TLSv1.3,CN=mediarouter.home,2044-04-12)] → 详情端口表 + 2 个蓝链。
  - 边界:手机 → {"ports":[]} → "无开放端口(常见于手机/平板)"。
  - 错误:大端口段中途取消 → 已发现端口保留至结果,标注"扫描中断"。
- **错误处理**:单台端口扫失败不影响其他设备;banner 超时为空不报错。

### 3.7 模块 M7 设备列表与详情(查看+改名主战场)

- **功能**:表格(默认)/卡片/详情三视图;搜索/过滤/排序;改名与备注;在线状态。
- **触发**:扫描完成或历史加载后渲染;用户交互。
- **流程**:GET /api/job(完成)或其他来源 → apply_meta(合并 friendly name)→ 渲染;搜索即时过滤(名称/IP/MAC/厂商/型号);过滤器:仅 Web 界面/有开放端口/新设备/已命名;排序:IP/类型/风险/名称;详情抽屉:识别字段全部 + 端口表 + 改名表单(名称+备注 + 保存/取消)。
- **处理规则**:名称/备注保存 → POST /api/device {mac, name, notes}(key 优先 MAC,无 MAC 用 IP,源码行为)→ 成功:行内立即更新 + toast;失败:toast 错误保留输入;保存中禁用按钮防重复。
- **边界**:设备重名允许(不强制唯一,提示可备注区分);空名称=清除名称;名称含 HTML → 引擎未转义(`[需实测]` 确认,前端 esc 处理);手机端表格列折叠(默认 IP/名称/厂商/状态,展开看全部)。
- **示例**:
  - 正常:改网关 → {mac:"30:a2:c2:77:9c:a6",name:"客厅主路由",notes:"华为 WS5800"} → {ok:true} → 行内"客厅主路由",重启服务后仍在。
  - 边界:只清空名称(传 null)→ 显示回默认(mdns_name/型号或"未知设备")。
  - 错误:mac/ip 均空 → 400 {"error":"mac or ip required"} → 前端保持表单并提示"设备身份缺失,请刷新后重试"。
- **错误处理**:保存失败(磁盘只读/权限)toast"保存失败,请检查数据目录权限" `[需实测]` 复现路径。

### 3.8 模块 M8 连接关系拓扑视图

- **功能**:视觉化"谁接入路由/谁在线"的连接关系。
- **触发**:视图切换(列表/卡片/拓扑)。
- **流程/规则**:拓扑数据由设备列表在前端派生(引擎无拓扑 API——事实):节点=设备(网关置中心,is_self 标记本机),边=网关-设备;布局=引擎内置(多布局+物理模拟,Obsidian 风格);在线/离线(最后结果内)配色区分;节点点击 → 详情抽屉(复用 M7)。
- **边界**:设备多(>50)→ 布局性能 `[需实测]`;无网关识别 → 中心节点=网段聚合占位;手机端小屏 → 拓扑全屏 + 双指缩放/单指拖动(engine 已实现则直接验证,`[需实测]`)。
- **示例**:
  - 正常:3 设备 → 中心"华为路由A2"连 2 节点(本机/手机),节点黄=在线,点击弹出详情。
  - 边界:20+ 设备 → 布局自动分散,防重叠(引擎物理布局),性能 `[需实测]`。
  - 错误:拓扑渲染异常(JS 错误)→ 降级为列表视图 + toast 提示"拓扑加载失败,已切换列表"。
- **错误处理**:fail-safe 降级,不让用户卡黑屏。

### 3.9 模块 M9 实时监控与通知(可选)

- **功能**:定时重扫 + 新设备检测 + 桌面通知(浏览器 Notification API)。
- **触发**:用户勾选"实时监控 5 分钟"(可选 1/2/5/10 分钟)→ 引擎定时重扫(单作业互斥,跳过重叠 tick)。
- **流程/规则**:每次完成:与最近基线/上次结果对比 → new_devices 徽标 + 通知(需浏览器授权;未授权 → 仅徽标);本模块通知=浏览器内(不做 webhook/MQTT,按范围决策)。
- **边界**:勾选即仅此浏览器会话生效;引擎重启 → 未勾选(默认);通知点击 → 聚焦该设备详情。
- **示例**:
  - 正常:勾选后 5 分钟 → 新手机接入 → 通知"发现新设备 192.168.3.88 · 未知设备(隐私地址)}"。
  - 边界:浏览器未授权通知 → 静默降级:仅列表"新设备"徽标,设置里可再次请求授权。
  - 错误:定时 tick 与手动扫描撞车 → 跳过本 tick 并在状态行提示"扫描进行中,本轮跳过"(引擎 busy 语义)。
- **错误处理**:监控循环异常 → 自动停止勾选并提示,不静默循环。

### 3.10 模块 M10 数据持久化与加载

- **功能**:meta/seen/历史/认证的原子读写与滚动清理;损坏容错。
- **触发**:任何写操作(保存名/备注/扫描完成/登录)。引擎实现:全部 `_load_json`/`_save_json`,原子替换(临时文件+rename)。
- **规则**:history 滚动保留 50 份(源码常量);meta/seen/auth 单文件持久;写入失败(权限/磁盘)返回值 false,调用方提示。
- **边界**:JSON 损坏 → 引擎 `_load_json` 回退默认值(不崩);数据目录不存在 → 首启创建;目录不可写(容器/权限场景)→ 功能静默失效(README 上游已知坑,启动器自检时检查可写 `[需实测]` 加检查)。
- **示例**:
  - 正常:改名 → devices.json 原子写入 `{"30:a2:c2:77:9c:a6":{"name":"客厅主路由","notes":...}}`。
  - 边界:history 51 份 → 最老删除,保留最新 50。
  - 错误:devices.json 语法损坏 → 回退 `{}` 并日志,界面"设备名标注已重置"(不崩溃)。
- **错误处理**:数据目录不可写 → 启动器自检警告 + UI 状态卡警示条。

### 3.11 模块 M11 日志与诊断

- **功能**:引擎标准输出日志(启动行/扫描行/错误 traceback)、HTTP 层错误暴露、诊断信息收集。
- **触发**:引擎生命周期与每次操作。
- **规则**:日志走 stdout/控制台启动器窗口;错误含 traceback;`/api/info` 提供自诊断(平台/CPU/网段/auth 状态);诊断包 = info 快照 + 最近日志(n 行 `[需实测]`)供反馈。
- **边界**:日志不落盘(上游行为,窗口关闭即丢)——启动器可选加 `> log.txt` 重定向(启动器层做,不改引擎);不记录密码/会话/完整设备 MAC(`[需实测]` 确认上游日志无敏感字段)。
- **示例**:
  - 正常:启动日志 `[netryx] serving on 0.0.0.0:8765 · data: netryx-data` → 控制台可见。
  - 边界:扫描异常 → traceback 打印,UI 照常可用。
  - 错误:HTTP 500 → 响应体含 error 字段,控制台 traceback。
- **错误处理**:诊断路径不暴露敏感数据(输出前过滤 token/password)。

### 3.12 模块 M12 异常与最坏情况矩阵(全局兜底)

| # | 最坏情况 | 现象 | 影响 | 兜底(自动/引导/文档) |
|---|---|---|---|---|
| 1 | 无管理员权限运行 | ARP/MAC 缺失、识别降级 | 识别不全 | 启动器提示"以管理员运行可获得完整 ARP";功能仍可用 |
| 2 | 防火墙拦 python | 本机也扫不到对外 | 全功能失效 | 启动器提示"允许专用网络";DEPLOY.md 写明;UI 检测 0 结果时提示检查防火墙 |
| 3 | 多网卡选错 | 扫到虚拟网段 | 看不到真设备 | 状态卡展示全部地址;扫描框可手改;DEPLOY 提示选 192.168.x.x |
| 4 | 断网/网关离线 | 本机 IP 回退 127.0.0.1 | 扫不了外网主机 | 状态卡"网络异常";本机仍可显示;提示连回路由 |
| 5 | 大网段/254+ 主机 | 扫描分钟级、端口全开更慢 | 等待焦虑 | 进度条+估算剩余;Quick 默认;Live monitor 间隔说明 |
| 6 | 前端缓存旧 ui.html | 改版不一致 | 交互混乱 | 版本号注入标题(`v2`)+ 强制刷新说明 |
| 7 | 默认密码未改 | 同网段他人可看 | 信息泄露 | 登录后常驻警告;启动器提醒;DEPLOY 强调 |
| 8 | 端口被占 8765 | 起不来 | 起不来 | 启动器检测提示;`--port` 备选 |
| 9 | 8765 未连通(手机) | 打不开 | 不可用 | 检查同一 WiFi/防火墙/IP 对(启动器打印地址) |
| 10 | 扫描中刷新/关页面 | 进度丢 | 体验差 | 作业在引擎内存继续,重进 attach |
| 11 | JSON 损坏/磁盘满 | 持久化失效 | 改名丢 | 回退默认+日志;启动器检查数据目录可写 |
| 12 | Python 缺失/版本旧 | 起不来 | 不可用 | 启动器提示安装 3.8+;DEPLOY 详述 |
| 13 | 现场网络复杂(多网段/企业 AP 隔离) | 扫不全 | 效果打折 | DEPLOY 写明"需同网段无线";拓扑以网关为中心说明 |
| 14 | 上游 netryx 更新破坏定制 | UI 改动丢失 | 返工 | UPSTREAM.md 清单对账;引擎原样可快速升版 |

---

## 4. 工具依赖清单

### 4.1 运行依赖(现场+实验)

| 依赖 | 版本 | 用途 | 来源 |
|---|---|---|---|
| Python | ≥3.8(本机 3.13.0 实测) | 引擎运行 | python.org,安装勾选 Add to PATH |
| netryx 引擎 | 锁定上游 commit(见 docs/UPSTREAM.md) | 扫描/服务 | MIT,零第三方包 |
| 操作系统 | Windows 10/11(现场首选;Linux/macOS 亦可) | 宿主 | — |
| 浏览器 | 手机 Chrome/Safari/Edge + 桌面任意 | 查看端 | — |

### 4.2 开发与验证工具(本机已有)

| 工具 | 用途 | 本机状态 |
|---|---|---|
| Python 3.13 + 标准库 | 开发/测试 | 已装(D:\app\python.exe) |
| PowerShell(pwsh) | 启动器/自动化 | 已装 |
| curl | 接口冒烟(登录/扫描/改名) | 已装 |
| playwright(已装 342 包内) | 390×844 手机视口截图与自动化验证 | 已装 |
| git + gh CLI | 版本管理/上游跟踪 | 已装(gh 2.97,登录 xzgjj) |
| Wireshark/tshark | (可选)抓包验证 mDNS/ARP 实际流量 | 已装 C:\Program Files\Wireshark |
| 真机手机 | 验收场景 B(必须) | 用户手机 |

### 4.3 可选(不阻塞交付)

- PyInstaller(netryx 自带 build.bat)→ 单文件 exe 交付(现场无 Python 时的备选;当前范围不需)。
- 全量 IEEE OUI 数据库下载(界面"Download full"→ 本方案隐藏按钮,默认内置 107 条,`[需实测]` 决定是否值得开放)。
- 2FA/TOTP(引擎已支持,默认不启用)。

---

## 5. 测试和约束

### 5.1 验收标准(Done 判定,对应 Q10)

1. 双击 start.cmd → 本机 `127.0.0.1:8765` 与手机同 WiFi `http://<IP>:8765` 均返回登录页(HTTP 200)。
2. 手机真机(390px 级)登录 → 中文表层文案;状态卡显示网段/网关。
3. 扫描完成:设备表含网关+接入设备+本机;网关带"路由/网关"徽标;端口列含 80/443 可点击 URL。
4. 拓扑视图:节点=设备,网关居中,可点击进详情。
5. 改名"XXX"→ 保存 → 行内更新 → 重启服务再扫描名称仍在。
6. `default_creds` 警告条显示,改密后消失。
7. 错误路径抽查:M1(无 Python 提示)、M4(空网段 400)、M7(mac/ip 空 400)→ 中文提示可见。
8. 仓库无真实环境数据(`shots/` 已 gitignore,提交列表核验)。
9. 现场冒烟演练:按 docs/DEPLOY.md 三步在"干净环境"(仅 Python)走通 `[需实测]`——实验家机为近似环境,现场真机前记录已知差。

### 5.2 测试矩阵(关键路径)

| 用例 | 步骤 | 预期 |
|---|---|---|
| T1 首次启动 | 删 netryx-data 后启动 | 登录 admin/admin;警告条出现;网段 192.168.3.0/24 预填 |
| T2 全流程扫描 | 扫 192.168.3.0/24 + Quick | 进度条推进;3+ 设备;统计卡数值一致 |
| T3 改名持久化 | 改名→重启→再扫 | 名称保留 |
| T4 手机视口 | playwright 390px 全流程 | 无横向溢出;核心操作可达 |
| T5 取消扫描 | 扫中 Stop | status stopped;统计卡标注 |
| T6 busy 语义 | 扫中重复提交 | attach 提示,无第二作业 |
| T7 错误网段 | 输入非法/空值 | 前端提示,不触发作业 |
| T8 防火墙拦阻(模拟) | 入站规则禁用 → 局域网访问 | "服务无响应"提示与处理指引 |
| T9 断网恢复 | 断开网卡再连 | 状态卡"网络异常"→ 恢复自动正常 |
| T10 无管理员 | 非管理员启动 | 识别降级但可用,提示出现 |

### 5.3 性能与数值参数(铁律 ①)

| 参数 | 值 | 状态 |
|---|---|---|
| 扫描 ping 超时 | 700ms/主机 | 引擎源码固定(已核验) |
| 端口扫描并发/超时 | 引擎内部 | `[需实测]`(Quick 254 台完成时长,实测参考 1-2 分钟) |
| job 轮询间隔 | 0.8s | `[需实测]` 调优(CPU 与感知平衡) |
| 历史保留份数 | 50 | 引擎源码固定(已核验) |
| 会话有效期 | 30 天 | 引擎源码固定(已核验) |
| 登录限速窗口/阈值 | 源码实现 | `[需实测]` 复现(429 文案依赖) |
| 全段+Full 端口耗时 | 数分钟/台 | `[需实测]`(文档标注) |
| 拓扑 50+ 节点渲染 | — | `[需实测]` 确定上限与降级阈值 |

### 5.4 实现可行性评审(review 结论)

- **引擎层**:纯标准库已实测(启动/扫描/端口/识别/改名全链路通过);接口契约与数据结构全部从源码核验,SPEC 中示例与实际输出一致(本机真实扫描数据)。
- **定制层**:全部改动集中在 ui.html(裁剪+中文+移动适配,已部分验证)+启动器(独立文件)+文档;引擎零改动 → 升级路径清晰(UPSTREAM.md 对账)。
- **风险最高点**:①默认口令 + 0.0.0.0 暴露(已定:登录保留+双重提醒)②中文 ipconfig 网关正则兼容性(实测 CLI 网关识别成功,兜底=is_gateway 设备/状态卡"-")③真机手机适配(已知视图无横向溢出,按钮修复已做;保留真机验收)④现场环境差异(DEPLOY 冒烟+最坏矩阵)。
- **结论**:可完整落地;未被"把用户原话变成子任务"限制——交互路径、状态变化、反馈语言、接口/数据契约、兜底矩阵均为本设计补全,直接指导实现。
